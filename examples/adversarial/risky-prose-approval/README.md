# Risky Prose Approval

Tests a destructive tool that says approval is needed but lacks machine-readable approval metadata.

Expected result: GD018.

Why it matters: tool harness code needs fields it can inspect, not just prose in a description.
