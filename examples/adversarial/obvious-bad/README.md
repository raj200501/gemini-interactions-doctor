# Obvious Bad

Tests a compact fragile Gemini support workflow.

Expected result: `NOT_READY` with GD013, GD014, GD015, GD006, GD008, and GD009-like findings.

Why it matters: the scanner should catch the prototype-to-workflow failure mode without relying only on the main fragile example.
