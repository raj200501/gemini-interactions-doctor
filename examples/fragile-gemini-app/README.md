# Fragile Gemini App

This intentionally brittle sample is used by `gdoctor demo`.

It models a support helper that grew from a one-shot Gemini call into a workflow with chat history, tools, JSON parsing, and a "verified answer" banner, but without the harness pieces needed for reliable iteration.
